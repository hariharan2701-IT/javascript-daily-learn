var number1 = 1000;
var number2 = 66;
var number3 = 34;  // New third variable
var result;
var findTotal;

findTotal = function () {
    result = number1 + number2 + number3;  // Update to add three numbers
};

findTotal();

console.log(number1 + " + " + number2 + " + " + number3 + " = " + result);  // Update to show the new calculation
